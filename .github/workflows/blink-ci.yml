name: AVR Blink

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: acrandal/arduino-cli:latest

    steps:
    - uses: actions/checkout@v2
    - name: build
      run: |
        cd Blink
        make
